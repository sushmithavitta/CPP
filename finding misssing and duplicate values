#include<bits/stdc++.h>
using namespace std;
void dup(int arr[],int n){
    sort(arr,arr+n);
    for(int i=0;i<n;i++){
        if(arr[i]!=i+1){
            cout<<"["<<i+1<<","<<arr[i]<<"]";
        }
        
    }
    
}
int main(){
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>> arr[i];
    }
    dup(arr,n);
    return 0;
    
}
